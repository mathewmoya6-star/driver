{
  "name": "mei-drive-africa",
  "version": "1.0.0",
  "description": "Kenya's Premier Driver Safety Platform",
  "type": "module",
  "scripts": {
    "start": "npx serve . -l 8080",
    "dev": "npx live-server --port=3000",
    "seed": "node scripts/seed-database.js",
    "upload-images": "python upload_media.py",
    "build": "echo 'Static site - no build needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.0",
    "sharp": "^0.33.0"
  }
}
